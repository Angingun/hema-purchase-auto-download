# ============================================================
# 配置文件 - 按需修改以下参数
# ============================================================

# ── WebBridge daemon ────────────────────────────────────────────────
WEBBRIDGE_PORT = 10086  # daemon 端口，默认 10086

# ── 目标网站 ────────────────────────────────────────────────────────
# 盒马门户首页（仅参考，实际直连下方 iframe）
LOGIN_URL = "https://portalpro.hemaos.com/?storeTag=STORE_MANAGEMENT"
# 采购单列表页面（iframe 内的实际内容页）
PURCHASE_LIST_URL = "https://portalpro.hemaos.com/pages/supplierPlatformNew/purchaseList.html"

# ── 下载文件保存目录（绝对路径）────────────────────────────────────
# 注意：WebBridge 通过真实 Chrome 下载，默认保存到系统「下载」文件夹。
# 如需自定义路径，脚本会尝试通过 CDP 设置；若不支持则回退到此路径。
DOWNLOAD_DIR = r"C:\Users\Qingrun\Desktop\采购单导出"
# Chrome 默认下载目录（备选，CDP 设置失败时使用）
CHROME_DOWNLOADS_DIR = r"C:\Users\Qingrun\Downloads"

# ── 查询参数 ────────────────────────────────────────────────────────
SUPPLIER_KEYWORD = "282265890"                    # 供应商搜索关键词
SUPPLIER_NAME = "KA佳农食品(上海)有限公司（新）"   # 供应商下拉选项文字

# ── 日期配置 ────────────────────────────────────────────────────────
# 要求到货日期（主日期，在 config 中修改这两个值即可）
# 留空则自动使用今天往前 7 天 ~ 今天
DELIVERY_DATE_START = "2026-06-06"   # 格式: "2026-06-16"，留空自动计算
DELIVERY_DATE_END   = "2026-06-12"   # 格式: "2026-06-23"，留空自动计算

# 创建日期 = 要求到货 start - CREATE_OFFSET_DAYS 天，end = 要求到货 end
CREATE_OFFSET_DAYS = 7  # 创建日期向前偏移天数

# ── 采购单状态（页面多选下拉中的选项文字）─────────────────────────
# 如需修改要勾选的状态，调整此列表
PURCHASE_STATUS_WANTED = [
    "审核通过",
    "部分发货",
    "发货完成",
    "全部入库",
    "部分入库",
]

# ── 操作延迟（秒），网速慢可适当增大 ────────────────────────────────
DELAY_SHORT  = 0.8   # 短暂等待
DELAY_MEDIUM = 2.0   # 页面/接口加载
DELAY_LONG   = 4.0   # 大数据查询加载
DELAY_DOWNLOAD = 5.0 # 等待文件下载完成

# ── 最大翻页数（防止死循环，按需调大）──────────────────────────────
MAX_PAGES = 50
